import LoadingScreen from './components/ui/LoadingScreen';

/** Global route-transition fallback: žaluzieee logo loader (ported from ceniky-admin-2). */
export default function Loading() {
  return <LoadingScreen />;
}
